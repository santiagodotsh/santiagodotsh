---
interface Props {
	title?: string
}

const { title } = Astro.props
---

<section class='w-full h-auto max-w-2xl space-y-2.5'>
  {title && (
    <h2 class='font-sans text-slate-100 text-xl font-bold'>
      {title}
    </h2>
  )}
  <slot />
</section>